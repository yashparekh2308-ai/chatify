# Frontend environment variables

# VITE_API_URL is required for production deployments (e.g., Vercel)
# to tell the frontend where the backend server is hosted.
# For local development, this is not needed as Vite proxies '/api' to localhost.
VITE_API_URL=http://localhost:3000
